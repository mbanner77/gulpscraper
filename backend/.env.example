# Beispiel-Umgebungsvariablen für die lokale Entwicklung
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an

# Allgemeine Konfiguration
DATA_DIR=data
FRONTEND_URL=http://localhost:3000

# E-Mail-Konfiguration
EMAIL_RECIPIENT=your-email@example.com
EMAIL_SENDER=noreply@example.com
EMAIL_SUBJECT=Neue GULP Projekte gefunden!

# Cloud-Umgebung (wird automatisch von Render.com gesetzt)
# RENDER=true
# CLOUD_ENV=true

# Headless-Modus für Playwright (true/false)
HEADLESS=true
